# Frontend
frontend/node_modules/
frontend/dist/

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Rust/WASM build cache — regenerate with `npm run build:wasm` (in frontend/).
# frontend/wasm/pkg/ (the compiled glue + .wasm binary) IS committed: it is
# what lets the site deploy without a Rust toolchain in the build environment.
wasm/target/

# Local editor/assistant config — never belongs in the repo.
.claude/
